<?xml version="1.0" encoding="utf-8"?>
<!-- Wider/taller default than the today widget so the 7-day bar chart has room. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="250dp"
    android:minHeight="110dp"
    android:targetCellWidth="3"
    android:targetCellHeight="2"
    android:minResizeWidth="180dp"
    android:minResizeHeight="80dp"
    android:maxResizeWidth="460dp"
    android:maxResizeHeight="240dp"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:description="@string/widget_week_desc"
    android:previewImage="@drawable/widget_preview_week"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/glance_default_loading_layout" />
